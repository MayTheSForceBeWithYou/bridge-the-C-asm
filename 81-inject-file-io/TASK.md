# 81 — Inject file I/O (practice)

Read `LESSON.md` first. This file is only the lab.

## Do

1. `make` && `./prog` — exit 0.
2. Skim `ch24/config.c` `config_load` (read-only). Find the `fopen`.
3. Optionally fill `student_parse.c` and relink.

## Done when

- Fake-fed counts match the planted text.
- You can explain why the parser function's signature mentions `FileOps`.

## Lookup

`man 3 strtok_r`, lesson 80.
