#ifndef MY_CHECK_H
#define MY_CHECK_H

#include <stdio.h>

/* TODO: static counters tests_run and tests_failed */

/* TODO: CHECK(expr) — bump run count; on failure bump fail count and
   print __FILE__ / __LINE__ / #expr */

/* TODO: CHECK_EQ(actual, expected) — compare ints; print both values
   on failure */

/* TODO: test_report(void) — print summary; return non-zero if any failed */

#endif
