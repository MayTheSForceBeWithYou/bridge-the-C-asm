#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    /* TODO: change this message, rebuild the full pipeline, and re-inspect. */
    char message[] = "GET FUCKED!\n";
    printf("%s", message);
    return 0;
}
